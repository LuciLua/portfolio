export const en = require("../../public/lib/en.json")
export const pt = require("../../public/lib/pt.json")
export const fr = require("../../public/lib/fr.json")

export const dictionaries = () => {
  return { en, pt, fr }
}
